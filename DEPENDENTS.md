# Dependents

Projects that depend on **morpha**. Update them after changes to the public API,
entry points, or package metadata.

| Project | Path | Mechanism |
|---------|------|-----------|
| geonexus | `work/research/geonexus` | editable pip install (`environment.yml`) |
